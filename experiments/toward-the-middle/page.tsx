import type { Metadata } from 'next';
import Client from './Client';

export const metadata: Metadata = {
  title:
    'Toward the Middle: The Prior You Never Agreed To, Measured on Your Own Sense of Duration, and Why Removing It Would Make You Worse',
  description:
    "A real Bayesian central tendency experiment, the observer model of Jazayeri and Shadlen 2010 fitted by maximum likelihood to your own interval reproductions, narrated by an AI that has none of this because when it needs a duration it reads a number and the number is the number. The task is as simple as this lab gets: two flashes arrive with a gap between them, and you press once when the same gap has passed again. No timer, no counting, no feedback, about a hundred times. You will fail in a specific direction. Short intervals come back too long, long ones too short, everything dragged toward the middle of the set you have been shown, and the size of that pull is not sloppiness but the exact amount an observer would choose if it were mixing a noisy measurement with everything it had quietly learned about which intervals happen here. The design is the trap. Two halves, in an order decided by a coin flip, one drawing intervals from 600 to 1000 ms and the other from 900 to 1300 ms, and 900 and 1000 appear in both: physically identical, the same code on the same screen minutes apart, the long ones in one half and the short ones in the other. If what you report is the interval, they come back the same. If what you report is the interval blended with the company it keeps, they do not, and nothing on the page tells you which half you are in until the results. The model is the Bayesian least squares observer with scalar noise, the shape timing noise actually has, three parameters fitted over a numerically integrated measurement grid with a lapse mixture, and it is fitted jointly across both halves because noise and lag belong to the person while the prior belongs to the block. That is what makes the overidentification check possible: refit on the short half alone, then predict every mean in the long half with no free parameters and no sight of that data, including two intervals whose reproductions the fit has never seen. The calibration honesty is a theorem rather than a disclaimer. Every quantity here is an interval between two events on one clock, never a latency, so a constant lag anywhere from photons to key scan lands in the free offset parameter, and a constant shifts an intercept and cannot touch a slope. What can reach the answer is jitter rather than lag, and its direction is stated: jitter inflates the measurement noise, inflated noise predicts MORE compression, so a run where your bias exceeds the prediction is not something the machine could have manufactured. Frame intervals are measured live and printed, flash onsets come from the frame after the paint request, and the delivered interval rather than the requested one goes into every fit. The control that had to be here is serial dependence, because the previous trial is a member of the same set and a pull toward the set looks like a pull toward the last one from a distance, so the residuals are regressed on the previous interval and the partial slope is printed with its own interval. Guards run live: presses before the second flash abort and re-append, anticipations and lapses are counted and excluded, trials whose stimulus window contained a dropped frame are thrown out, the first six trials of each half are dropped as prior learning with the analysis re-run including them, a thin run is refused a headline and named an absent measurement, and a fitted blur under 0.06 is flagged as probable counting, which is not cheating but does change what was measured, since an exact measurement leaves a prior nothing to correct. The last card is the reason it is worth eight minutes. Your reproductions are systematically wrong, so the page simulates the observer you would rather be from your own fitted noise, honest and unbiased and pulled toward nothing, and computes the error it would have made. The honest one is worse. The bias is not a flaw sitting on top of good machinery, it is the machinery. You never see the raw measurement, not once, not in this experiment and not in the rest of your life. Everything runs in your browser, nothing is recorded and nothing leaves the page.",
};

export default function Page() {
  return <Client />;
}
